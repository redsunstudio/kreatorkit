/**
 * Who made this. Kept in a plain module (no JSX) so server code — emails,
 * metadata — can name the maker without pulling a React component in.
 * UI surfaces render it through `components/created-by.tsx`.
 */
export const CREATED_BY_NAME = 'John Isaacson';
export const CREATED_BY_URL = 'https://johnisaacson.co.uk';
